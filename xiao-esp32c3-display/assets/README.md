# assets （[日本語](README_JP.md)）

This directory is not used by the current APOD display version.

APOD images are saved under `../image-render/cache/` on the Mac side and converted for the e-paper display by `../image-render/render_apod_image.py`.

Place fixed logos or boot images here if they are added in the future.
