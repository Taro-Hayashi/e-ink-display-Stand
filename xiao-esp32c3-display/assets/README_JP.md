# assets （[English](README.md)）

このディレクトリは現在のAPOD表示版では使用しない。

APOD画像はMac側の `../image-render/cache/` に保存され、`../image-render/render_apod_image.py` で電子ペーパー向けに変換される。

将来、固定のロゴや起動画面画像を追加する場合は、このディレクトリに配置する。
